const Article = require('../models/article');


const createArticle = async (name, current,exhibitors,gameBookedList,space) => {
    /*
    try {
        const article = new Article({
            name, current,exhibitors,gameBookedList,space //TODO: wrong arguments here
        });
        console.log(article);
        return await article.save();
    } catch (error) {
        console.log(error);
        throw error;
    }
    */
    console.log("articleController: createArticle OK")
    return
};

const deleteArticle = async () => {
    console.log("articleController: deleteArticle OK")
    return
}